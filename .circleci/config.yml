version: 2.1
orbs:
  # orgs contain basc recipes and reproducible actions (install node, aws, etc.)
  node: circleci/node@5.0.2
  eb: circleci/aws-elastic-beanstalk@2.0.1
  aws-cli: circleci/aws-cli@3.1.1
  # different jobs are calles later in the workflows sections
jobs:
  build:
    docker:
      # the base image can run most needed actions with orbs
      - image: "cimg/node:14.15"
    steps:
      # install node and checkout code
      - node/install:
          node-version: "14.15"
      - checkout
      # Use root level package.json to install dependencies in the frontend app
      - run:
          name: Install Front-End Dependencies
          command: |
            echo "NODE --version" 
            echo $(node --version)
            echo "NPM --version" 
            echo $(npm --version)
            npm run frontend:install
      # TODO: Install dependencies in the the backend API
      - run:
          name: Install API Dependencies
          command: |
            npm run api:install
      # TODO: Lint the frontend
      - run:
          name: Front-End Lint
          command: |
            npm run frontend:lint
      # TODO: Build the frontend app
      - run:
          name: Front-End Build
          command: |
            npm run frontend:build
      # TODO: Build the backend API
      - run:
          name: API Build
          command: |
            npm run api:build
            printenv > www/.env
            npm run archive
      - aws-cli/install
  # deploy step will run only after manual approval
  deploy:
    docker:
      - image: "cimg/base:stable"
      # more setup needed for aws, node, elastic beanstalk
    steps:
      - node/install:
          node-version: "14.15"
      - eb/setup
      - aws-cli/setup
      - checkout
      - run:
          name: Deploy App
          # TODO: Install, build, deploy in both apps
          command: |
            eb setenv POSTGRES_HOST= udagram\.ca3gpk5m5mjg\.us\-east\-1\.rds.amazonaws\.com
            eb setenv DB_PORT=val5432ue 
            eb setenv POSTGRES_USERNAME=postgres 
            eb setenv POSTGRES_PASSWORD=password123 
            eb setenv POSTGRES_DB=postgres 
            eb setenv JWT_SECRET=IreliaTop 
            eb setenv URL=http\:\/\/udagram\-api\-dev\.eba\-azp325wt\.us\-east\-1\.elasticbeanstalk\.com\/ 
            eb setenv AWS_BUCKET=ashraf\-udagram
            eb setenv AWS_REGION=us\-east\-1 
            eb setenv AWS_PROFILE=default 
            eb setenv AWS_ACCESS_KEY_ID=AKIAZZUQPF4ORMSI64OA
            eb setenv AWS_SECRET_ACCESS_KEY=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
            npm run deploy

workflows:
  udagram:
    jobs:
      - build
      - hold:
          filters:
            branches:
              only:
                - master
          type: approval
          requires:
            - build
      - deploy:
          requires:
            - hold
